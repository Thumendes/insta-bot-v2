export const TMP_DIR = `${process.cwd()}/tmp`;

export const CONFIG_FILE = `${TMP_DIR}/config.json`;

export const STATE_FILE = `${TMP_DIR}/state.json`;

export const EXTENSION_PARSER_TYPES = {
  JSON: "json",
  TXT: "txt",
};
